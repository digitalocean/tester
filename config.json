{
  "packages": [
    {
      "name": "db",
      "path": "/bin/db.test",
      "options": [
        {
          "name": "test.timeout",
          "description": "Maximum time tests can run for",
          "default": "1m"
        }
      ]
    },
    {
      "name": "http",
      "path": "/bin/http.test",
      "options": [
        {
          "name": "test.timeout",
          "description": "Maximum time tests can run for",
          "default": "1m"
        }
      ]
    }
  ],
  "scheduler": {
    "run_timeout": "1m",
    "run_delay": "1m",
    "max_resets": 1
  },
  "slack": {
    "default_channels": [ "alerts" ]
  }
}
